<Window x:Class="NIKTOPIA.Views.InventoryView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NIKTOPIA.Views"
        mc:Ignorable="d"
        Title="InventoryView" Height="400" Width="400" Background="Beige" x:Name="inventoryView">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="INVENTORY" Grid.Column="0" Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center"   FontSize="24" />
        <ListBox Grid.Column="1" ItemsSource="{Binding Inventory}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Label Content="{Binding GameItem}"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
            </ListBox>
            <Label Grid.Column="2" x:Name="itemName"/>
    </Grid>
</Window>
